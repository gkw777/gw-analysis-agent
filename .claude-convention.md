# Feature-Based 폴더 구조

src/
├── assets/ # 공용 자산(icon, svg, font...)
├── shared/ # 🔒 모든 팀원이 사용 가능
│ ├── components/ # 공통 UI
│ │ ├── commons/ # 공용 UI(순수 MUI 컴포넌트) (Button, Modal, Card...)
│ │ ├── layouts/ # 공용 레이아웃 (header, gnb, content 영역...)
│ │ ├── specific/ # 특정한 컴포넌트 (각 feature에서 공통으로 사용가능한 컴포넌트. 예, 조직도 Modal, 목록이 같은 selectBox...)
│ ├── hooks/ # 공용 Hook (useToken, useUserInfo)
│ ├── store/ # 공용 상태관리 (userAtom, authAtom...)
│ ├── types/ # 공용 타입 (User, Auth...)
│ ├── utils/ # 공용 함수 (formatter, apiClient(axiosInstance)...)
│ ├── styles/ # 공용 스타일 (theme, global, variables...)
│ └── constants/ # 공용 상수
│
├── features/ # 🎯 Feature별 격리( 내부 root폴더는 예시일뿐 서비스에 맞게 폴더name 지정 )
│ ├── dashboard/
│ │ ├── components/ # Dashboard만의 컴포넌트
│ │ ├── hooks/ # Dashboard만의 Hook
│ │ ├── services/ # API 호출 관리
│ │ ├── types/ # Dashboard 타입
│ │ ├── store/ # 상태관리
│ │ └── DashboardPage.tsx # 진입점
│ │ └── index.tsx # page export
│ │
│ ├── products/ # 같은 구조
│ │ └── ...
│ │
│ └── settings/
│ └── ...
├── routers/ # 각 서비스 라우터
│
├── App.tsx
└── main.tsx

## 1. shared 폴더

- 추후 MFE 방식으로 변경될것을 대비하여 각 폴더별로 분리하고 index.ts로 export 처리. 단, root 폴더에만 index.ts 생성
- components/commons/ 폴더는 순수 MUI UI 컴포넌트만 적용하며, 필요한 정보는 interface(최대한 MUI interface props를 상속받아 모든 기능을 활용 및 custom(pick, Omit)활용 ) 정의하여 props로 전달받아 공용으로 사용할 수 있도록 처리한다.

# 팀 프로젝트 컨벤션

## 1. 폴더 구조 규칙

✅ shared/ : 재사용 컴포넌트, Hook, 타입, 유틸
❌ features/ : 다른 feature import 금지

## 2. Import 규칙

✅ @/shared/* 접근 가능
✅ @/features/[자기feature]/* 접근 가능 (같은 feature 내부에서 2단계 이상 상위 폴더로 가는 상대경로 대체용)
✅ ./같은폴더 접근 가능
❌ @/features/다른feature 접근 금지

## 3. 파일 명명

- 폴더: camelCase/ (auth/)
- 컴포넌트: PascalCase.tsx (DashboardChart.tsx)
- Hook: camelCase.ts (useDashboardData.ts)
- 서비스: camelCase.ts (dashboardAPI.ts)
- 스토어: camelCase.ts (authAtom.ts)
- 타입: camelCase.ts (dashboard.ts)
- 상수: camelCase.ts

## 4. 상태관리

- 전역 상태: shared/hooks + Context/Zustand
- Feature 상태: features/[name]/store
- UI 상태: 컴포넌트 내부 useState

## 5. API 호출

- shared/hooks: useToken, useUserInfo
- features/[name]/services: API 호출 로직

## 6. TypeScript

- shared/types: 전역 타입
- features/[name]/types: Feature 타입

## 7. Export

shared/components/commons/index.ts
→ Button, Modal, Card 모두 export
features/dashboard/index.ts
→ DashboardPage만 export (내부 컴포넌트 X)

## 8. Claude AI 활용

- "이 컴포넌트가 shared 또는 feature별인지 판단해줄래?"
- "공통 컴포넌트로 추출할 부분이 있을까?"
- "이 import 경로가 규칙을 벗어나지 않나?"

## 9. PR 체크리스트

- [ ] shared/ 접근만 했는가?
- [ ] 같은 feature 내에서 import했는가?
- [ ] 파일이 올바른 폴더에 있는가?
- [ ] index.ts 파일이 업데이트되었는가?
